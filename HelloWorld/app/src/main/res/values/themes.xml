<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HelloWorld" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="appBackgroundColor">@color/white</item>
        <item name="headerBackground">@color/purple_accent</item>
        <item name="headerTextColor">@color/white</item>
        <item name="primaryTextColor">@color/purple_text</item>
        <item name="subHeaderBackground">@color/yellow_accent</item>
        <item name="subHeaderTextColor">@color/black</item>
    </style>

    <style name="Theme.HelloWorld" parent="Base.Theme.HelloWorld" />
</resources>