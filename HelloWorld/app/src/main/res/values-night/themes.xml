<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HelloWorld" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="appBackgroundColor">@color/dark_background</item>
        <item name="headerBackground">@color/purple_accent_night</item>
        <item name="headerTextColor">@color/purple_text_night</item>
        <item name="primaryTextColor">@color/purple_text_night</item>
        <item name="subHeaderBackground">@color/yellow_accent_night</item>
        <item name="subHeaderTextColor">@color/black</item>
    </style>
</resources>